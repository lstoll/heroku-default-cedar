# Heroku Default Cedar

Patches the `heroku create` command to make it default to the cedar stack. Nasty hack, but gets the job done.

## Installation

    heroku plugins install git://github.com/lstoll/heroku-default-cedar.git
